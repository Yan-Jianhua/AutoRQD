import warnings

warnings.filterwarnings('ignore')
from ultralytics import YOLO
import numpy as np

if __name__ == '__main__':
    model = YOLO(r'E:\PythonProject\***\runs\train\exp**\weights\best.pt')
    metrics = model.val(data='test.yaml',
              split='val',
              imgsz=640,
              batch=32,
              iou=0.5,
              rect=False,
              save_json=False,
              project='runs/test',
              name='exp',
              )
